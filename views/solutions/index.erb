<h1><%= @solutions.first.task.name %> &mdash; Solutions</h1>
<div class="row">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <td>Name</td>
        <td class="text-center">Points (Max. Possible - <%= @solutions.first.task.max_points %>)</td>
      </tr>
    </thead>
    <tbody>
      <% @solutions.each do |solution| %>
      <tr>
        <td>
          <a style="display: block;" href="/tasks/<%= solution.task_id %>/solutions/<%= solution.id %>">
            <%= solution.user.full_name %>
          </a>
        </td>
        <td class="text-center">
          <span><%= solution.points %></span>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
